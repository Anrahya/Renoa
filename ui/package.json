{
  "name": "@renoa/ui-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "build": "pnpm --filter @renoa/desktop build",
    "check": "pnpm check:web && pnpm check:native",
    "check:native": "cargo fmt --manifest-path desktop/src-tauri/Cargo.toml -- --check && cargo clippy --manifest-path desktop/src-tauri/Cargo.toml --all-targets -- -D warnings && cargo test --manifest-path desktop/src-tauri/Cargo.toml",
    "check:web": "pnpm lint && pnpm test && pnpm build",
    "desktop:dev": "pnpm --filter @renoa/desktop tauri:dev",
    "desktop:dev:local": "./scripts/desktop-dev.sh",
    "lint": "eslint \"desktop/src/**/*.{ts,tsx}\"",
    "test": "pnpm --filter @renoa/desktop test",
    "test:upstream": "pnpm --filter @acp-components/core test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "typescript-eslint": "^8.62.0"
  }
}
