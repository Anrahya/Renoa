{
  "name": "@renoa/integration-catalog",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0 OR MIT",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "node ./scripts/clean-dist.mjs && tsc",
    "check": "tsc --noEmit",
    "test": "pnpm run build && node --test --test-reporter=spec dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2"
  }
}
